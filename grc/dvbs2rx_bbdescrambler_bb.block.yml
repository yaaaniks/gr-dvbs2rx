# auto-generated by grc.converter

id: dvbs2rx_bbdescrambler_bb
label: BBdescrambler
category: "[Core]/Digital Television/DVB"
flags: [python, cpp]

parameters:
    - id: standard
      label: Standard
      dtype: string
    - id: framesize
      label: FECFRAME size
      dtype: string
    - id: rate
      label: Code rate
      dtype: string

inputs:
    - domain: stream
      dtype: byte

outputs:
    - domain: stream
      dtype: byte

templates:
    imports: from gnuradio import dvbs2rx
    make: |-
        dvbs2rx.bbdescrambler_bb(
            *dvbs2rx.params.translate(${standard},
                ${framesize},
                ${rate}
            )
        )

cpp_templates:
    includes: [
        "#include <gnuradio/dvbs2rx/bbdescrambler_bb.h>",
        "#include <gnuradio/dvbs2rx/common.h>"
    ]
    declarations: "gr::dvbs2rx::bbdescrambler_bb::sptr ${id};"
    make: |
        this->${id} = gr::dvbs2rx::bbdescrambler_bb::make(
            gr::dvbs2rx::string_to_dvb_standard["DVB-S2"],
            gr::dvbs2rx::string_to_dvb_framesize[${framesize}],
            gr::dvbs2rx::string_to_dvb_code_rate[${rate}]
        );
    link: ["gnuradio-dvbs2rx"]
    translations:
        "'": '"'
        "True": "true"
        "False": "false"
file_format: 1
